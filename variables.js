let car = {
  company: "BMW",
  model: "X7",
  year: 2021,
};

console.log(car, typeof car); //typeof is used to say what kind of data type is it, ex: string (or) number
console.log(car.model, typeof car.model);
console.log(car.year, typeof car.year);
